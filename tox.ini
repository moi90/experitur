[tox]
envlist = py{36,37,38}-{optional,no_optional}

[testenv]
usedevelop = True
passenv = TOXENV CI TRAVIS TRAVIS_* CODECOV_*
extras =
    tests
    optional: optional
deps =
    pytest
    codecov>=1.4.0
    pytest-cov
commands =
    pytest --cov=experitur
    codecov -e TOXENV
